# منصة البرمجة — ملف التسليم

اقرأ هذا الملف كاملاً قبل أن تكتب سطراً واحداً في هذا المشروع.

هو ليس وصفاً للكود — الكود يصف نفسه. هو **الأسباب**: لماذا بُني كل شيء
هكذا، وما الذي جُرِّب فأخفق، وما الذي لو غيّرته انكسر شيء لا تراه.

---

## ١. ما هذا المشروع

منصة تعليمية عربية (RTL) لمادة **«البرمجة والذكاء الاصطناعي»** — الصف
الثاني الثانوي، بكالوريا، مدارس اللغات بمصر.

- **المستخدمون:** مدرّس واحد (حسن الحوشي) وأقل من ٣٠ طالباً.
- **التقنية:** Next.js 16 (App Router) · React 19 · TypeScript · Tailwind v4
  · Supabase (قاعدة بيانات ومصادقة وتخزين) · Vercel.
- **القيد الحاكم:** كل شيء على **الباقات المجانية**. أي اقتراح يتجاوزها
  مرفوض بلا نقاش.

### المدرّس ليس مبرمجاً

يقرأ العربية ويفهم المنطق، ولا يقرأ الكود. فكل رسالة خطأ وكل نص في
الواجهة يجب أن يكون **بالعامية المصرية الواضحة** ويقول ما العمل، لا ما
حدث. و«حصلت مشكلة» وحدها ليست رسالة.

---

## ٢. القرارات المحسومة — لا تُناقَش ولا تُعاد

هذه ليست تفضيلات. كلٌّ منها قرار اتُّخذ ولا يُعاد فتحه:

| الأمر | القرار |
|---|---|
| المظهر | **فاتح فقط**. لا وضع ليلي، ولا حتى «للتجربة» |
| الحدود | `0.5px` بدل الظلال. `box-shadow` لحلقة التركيز وحدها |
| الزوايا | البطاقة `10px` · عناصر التحكم `6px` |
| الألوان | لا تدرّجات. ألوان الحالة **في الشارات الصغيرة فقط**، لا كخلفية بطاقة |
| الخط | IBM Plex Sans Arabic بأوزان 400/500/600 فقط |
| الأيقونات | Lucide بخطوط خارجية. **لا إيموجي كأيقونات إطلاقاً** |
| التسمية | «الفصل الأول · الدرس الثاني» دائماً. لا «1-2» أبداً |
| المقارنة | **لا لوحات صدارة ولا ترتيب بين الطلاب**. أبداً |
| المال | لا اشتراكات ولا أسعار ولا مدفوعات. المنصة ليست منتجاً |
| الرسائل | لا بريد ولا SMS |
| الحذف | الأرشفة أولاً. الحذف النهائي للمؤرشف الفارغ فقط |

**عن لوحة الصدارة تحديداً:** طُلبت من منصة منافسة وقُوبلت بالرفض. السبب
ليس تقنياً: الطالب الضعيف الذي يرى نفسه في ذيل ترتيب يترك المنصة، لا
يذاكر أكثر.

---

## ٣. نموذج الأمان — أخطر ما في المشروع

المطلب الأصلي من المدرّس، بنصّه:

> الطالب لازم يكون **مستحيل** عليه يشوف الإجابات الصحيحة بأي طريقة، حتى لو
> فتح DevTools أو شاف الـ network requests أو عدّل الطلبات يدوياً.
> الإجابة الصحيحة متتبعتش للمتصفح إطلاقاً أثناء حل الامتحان، من غير أي حقل
> يدل عليها **لا مباشرة ولا ضمنياً** (ترتيب ثابت أو حقل مخفي أو ID بيفضح
> الإجابة). التصحيح على السيرفر بالكامل. **إخفاء عنصر من الواجهة مش حماية.**

هذه الجملة الأخيرة هي القاعدة. كل ما يلي تطبيق لها.

### ٣-١ الفصل البنيوي

`question_options` **لا يوجد فيه عمود `is_correct` إطلاقاً**. الصحة في
جدول منفصل `question_keys` عليه سياسة RLS واحدة: **المدرّس فقط**.

هذا مقصود بنيوياً لا إجرائياً: لا يمكن أن يُسرَّب ما لا وجود له في الجدول،
ولا يعتمد الأمان على أن يتذكّر أحدٌ استبعادَ عمود من `select`.

`question_keys` يحمل ثلاثة أسرار: `key` (المفتاح) و`model_answer`
(الإجابة النموذجية للمقالي) و`explanation` (الشرح). الثلاثة ترث السياسة
نفسها. **أي سرّ جديد يذهب هنا، لا إلى `questions`.**

### ٣-٢ التصحيح كله داخل Postgres

لا سطر تصحيح واحد في المتصفح. `submit_exam` و`check_bank_answer` دالّتان
`security definer`، وكلتاهما تنادي `grade_one` المشتركة.

**`grade_one` مسحوبة من الجميع — لا `anon` ولا `authenticated`:**

```sql
revoke all on function public.grade_one(uuid, jsonb) from public, anon, authenticated;
```

السبب: دالة تقول «صح أم خطأ» عن سؤال بعينه هي **آلة تخمين**. من ناداها
بسؤال امتحان استخرج مفتاحه سؤالاً سؤالاً، بل أصاب بالتجريب بلا أن يرى
المفتاح أصلاً. لا تمنح هذه الدالة لأحد. أبداً.

**تحقّق أن هذا ما زال قائماً:**

```sql
select has_function_privilege('authenticated', 'public.grade_one(uuid, jsonb)', 'execute');
-- يجب أن يكون false
```

### ٣-٣ بوابة البنك

`check_bank_answer` هي الواجهة الوحيدة التي تصحّح سؤالاً منفرداً للطالب،
وأول ما تفعله:

```sql
if v_exam.kind <> 'bank' then
  raise exception 'NOT_A_BANK_QUESTION' using errcode = '42501';
end if;
```

**هذا السطر هو أخطر سطر في المشروع.** بدونه يصير البنك بابَ استخراجٍ
لمفاتيح الامتحانات. لا تحذفه ولا تخفّفه ولا تنقله إلى الواجهة.

### ٣-٤ البعثرة وقت التخزين لا وقت العرض

صفحة الحل ترسل `question_options.position` إلى المتصفح. فلو خُزّنت خطوات
سؤال «ترتيب» بترتيبها الصحيح لقرأ الطالب الإجابة من طلبات الشبكة — وهو
بالحرف ما حذّر منه المطلب: «ترتيب ثابت يفضح الإجابة».

فالبعثرة تحدث مرة واحدة **عند الاستيراد** في
[`src/lib/question-plan.ts`](src/lib/question-plan.ts). بعثرةُ العرض لا
تكفي: الترتيب المخزَّن يبقى كما هو وهو الذي يصل المتصفح.

ما يُبعثر: خطوات `ordering`، ومصطلحات `matching`، وعناصر `classification`
(لأن المؤلف يكتبها مجمّعة حسب سلّتها فيخمّن الطالب من التجاور).

### ٣-٥ المفاتيح تشير بالمعرّفات لا بالأرقام

مفتاح الاختياري `{"option_ids": ["<uuid>"]}` لا `{"index": 3}`. المعرّف
عشوائي فلا يدل ترتيبه على شيء.

### ٣-٦ التخزين بلا سياسات

`storage` لا سياسة RLS عليه إطلاقاً. كل رفع وتنزيل يمر بروابط موقّعة
تصدر من السيرفر بعد التحقق. لا تضف سياسة تخزين — أضف تحققاً في الفعل.

### ٣-٧ ما يجب أن يبقى صحيحاً

| الثابت | كيف تتحقق |
|---|---|
| صفر مفاتيح تصل الطالب والإظهار مقفول | `08_smoke_test.sql` — 13/13 |
| الطالب لا يرى محتوى غير مصرّح له | `07_security_check.sql` — 15/15 |
| الإجابة النموذجية لا تصل قبل الإظهار | `11_model_answer_check.sql` — 5/5 |
| البنك لا يصحّح سؤال امتحان | `19_bank_security_check.sql` — 15/15 |
| الإتقان لا يُسحَب والنسيان يُسجَّل | `24_bank_levels_check.sql` — 11/11 |
| البعثرة تعمل والمفتاح متسق معها | `check-question-types.ts` — 28/28 |

**شغّلها كلها قبل أي دفع.** الأرقام أعلاه هي المتوقَّع.

---

## ٤. خريطة المشروع

```
src/
  app/
    (student)/        صفحات الطالب — الرئيسية والمحتوى والبنك والنتائج
      exams/[examId]/ محرّك حل الامتحان (exam-runner + question-input + essay-input)
      bank/           بنك الأسئلة وجلسة التدريب
    admin/            لوحة المدرّس
    actions/          Server Actions — كل كتابة تمر من هنا
    files/            تنزيل PDF برابط موقّع
    answer-image/     عرض صورة إجابة برابط موقّع
  components/         مشتركات الواجهة (primitives.tsx فيه Badge وPageHeader…)
  lib/
    validation.ts     مخطط zod لملف الأسئلة + قواعد لا يعبّر عنها zod
    question-plan.ts  تحويل سؤال إلى صفوف + البعثرة + بناء المفتاح
    exam-draft.ts     مسودة الإجابات في localStorage
    format.ts         كل التسميات العربية ودوال الأسماء
    types.ts          الأنواع — يجب أن تطابق SQL (المدقّق يفرض ذلك)
  proxy.ts            ← ليس middleware.ts. انظر §7

supabase/
  sql/                21 ملفاً مرقّماً — تُنفَّذ بالترتيب
  check-sql.mjs       مدقّق ثابت
scripts/              فحوص تعمل على المشروع الحقيقي
curriculum/           كتاب الوزارة — خارج git (الريبو عام)
```

### قاعدة ملفات SQL

مرقّمة ومتراكمة. **لا تعدّل ملفاً قديماً لتغيّر سلوكاً** — أضف ملفاً
جديداً برقم أعلى. الاستثناء الوحيد: `01_schema.sql` يجب أن يبقى **مكتفياً
بذاته** ليعمل التنصيب النظيف، فأي `enum` أو عمود جديد يُضاف إليه أيضاً.
مدقّق `check-sql.mjs` يفرض هذا ويرفض غيره.

---

## ٥. الجزء التعليمي — البنية والمعنى

```
فصل (chapter) ← دروس (lessons) ← ملفات PDF + عناصر أسئلة (exams)
```

`exams.kind` ثلاث قيم، وهي **نفس الجدول** بثلاث طرق لعب:

| القيمة | العربية | السلوك |
|---|---|---|
| `practice` | تدريب | محاولة واحدة، تسليم، درجة |
| `exam` | امتحان | نفسه، بنبرة أشد في التحذيرات |
| `bank` | بنك أسئلة | **بلا محاولة ولا مؤقّت ولا تسليم** — يجيب فيعرف فوراً |

و`chapters.kind` و`lessons.kind` فيهما `review` لحاويات المراجعة العابرة
للفصول («ختام الفصلين الأول والثاني») — لا تأخذ رقماً في العرض.

### حالة الطالب في البنك — أربع حالات من عمودين

`bank_progress` يحمل `state` و`forgot`، ومنهما:

| الحالة | التمثيل | تظهر للطالب |
|---|---|---|
| ما شافوش | لا صف | في الجلسة العادية بعد الغلط |
| محتاج شغل | `wrong` | **أول** الجلسة القادمة |
| مثبَّت | `correct` | في المراجعة وحدها |
| محتاج مراجعة | `correct` + `forgot` | أول جلسة المراجعة |

**`state` لا ينزل أبداً.** من صحّت إجابته مرة يبقى مثبَّتاً وإن عاد فأخطأ:
العدّاد لا يتراجع ولا نسبة تقلّ، فلا يُعاقَب على أنه راجع. و`forgot` هي
التي ترتفع، فيعود السؤال بلا أن يخسر الطالب شيئاً — وهذا هو الفرق بين
«لم يفهم» و«نسي» عند المدرّس أيضاً: الأول يستدعي إعادة شرح والثاني تمريراً
سريعاً. و`forgot` وصفٌ لآخر إجابة لا سجلٌّ دائم: تُطفأ إذا أصلحه.

**الغلط لا يعود في الجلسة نفسها.** سؤالٌ بعد ثانيتين من رؤية إجابته يقيس
ذاكرة ثانيتين لا فهماً. يعود أول الجلسة القادمة.

### ترتيب جلسة البنك

الجلسة خمسة وعشرون سؤالاً، بترتيبين:

- **العادية** — غلط، ثم منسيّ، ثم لم يُرَ، ثم مُتقَن. وجوه كل رتبة صعودٌ من
  التعريف إلى التطبيق إلى التفريق.
- **المراجعة** (`?mode=review`) — المُتقَن وحده، ومن **فوق**: تفريق ثم تطبيق
  ثم تعريف، والأقدم أولاً داخل كل مستوى. من قدر على أسئلة التفريق فاهمٌ ولا
  يحتاج إعادة التعريفات فيقفل بعد قليل؛ ومن وقع فيها ينزل إلى ما تحتها.
  وهذا ما يجعل مراجعة درسٍ بعد ستة أشهر عشرة أسئلة لا اثنين وعشرين.

و`spreadByLesson` توزّع الجلسة على الدروس المختارة بالتناوب: بدونه يأكل
الدرس الأول الخمسة والعشرين، فمن اختار فصلاً بستة دروس راجع درساً واحداً
وظنّ أنه راجع الفصل.

### فلتر `/bank/select` بلا حالة خاصة

الصفحة ترسم الشجرة الموجودة في قاعدة البيانات كما هي: كل درس تشيك بوكس،
وتشييك الفصل يشيّك دروسه. ولذلك **مراجعة ختام الفصل ليست حالة خاصة** — هي
درس نوعه `review` فتظهر صفاً مثل غيرها ويستطيع الطالب أخذها وحدها.

وحاوية المراجعات (`chapters.kind = 'review'`) تظهر مجموعةً أختاً للفصول لا
داخلها، وهذا هو **سبب وجود الصفحة**: من شيّك الفصول الأربعة الأولى لا تصله
أسئلة ختام الترم أبداً لأنها ليست ابنة أي فصل منها، ولا يوجد فيها سؤال
يربط الفصل الأول بالرابع أصلاً — وهو ما يُسقِط الطالب في الامتحان.

### أنواع الأسئلة الثمانية

`mcq_single` · `mcq_multi` · `true_false` · `fill_blank` · `essay` ·
`matching` · `ordering` · `classification`

**الأخيرة ثلاثة سؤال واحد في ثلاثة أثواب:** «لكل عنصر، اختر قيمة». مفتاح
واحد `{"assign": [...]}` وفرع تصحيح واحد ومكوّن واجهة مشترك. لو أضفت
نوعاً رابعاً من هذه العائلة فاتبع النمط نفسه ولا تفتح فرعاً جديداً.

**الدرجة الجزئية:** `points × (الصحيح ÷ الكل)` في `fill_blank` والثلاثة.
و«كامل أو صفر» في الاختياري وصح/خطأ. قاعدة واحدة للطالب أسهل من قاعدتين.

**المقالي ممنوع في البنك** (`ESSAY_NOT_IN_BANK`): قيمة البنك في الرد
الفوري وهو لا يملكه، وإدخاله يُغرق طابور تصحيح مدرّس وحيد بشغل تدريب.

### الواجهة: لا سحب ولا إفلات

أغلب الطلبة على الموبايل، والسحب باللمس يتنازع مع تمرير الصفحة — يحاول
الطالب سحب عنصر فتتحرك الصفحة تحته والمؤقّت شغّال.

- `matching` و`classification`: قوائم منسدلة.
- `ordering`: **سهما فوق/تحت**. القائمة المنسدلة بالأرقام تسمح بإعطاء رقم
  واحد لعنصرين فيقع الطالب في حالة باطلة عليه هو أن يحلّها؛ الأسهم لا
  تستطيع إنتاج ترتيب باطل.

### تطبيع العربية

`normalize_ar` في SQL: تزيل التشكيل والتطويل، وتوحّد `أ إ آ ٱ ← ا` و
`ة ← ه` و`ى ← ي` و`ؤ ئ ← ء`، وتحوّل الأرقام العربية إلى لاتينية وتضغط
المسافات. فتُقبل «الذكاء الإصطناعى» أمام «الذكاء الاصطناعي».

**الخطأ الإملائي الحقيقي يبقى مرفوضاً عمداً** («التشفيير»). التسامح الذي
يبلغ ذلك الحد يقبل إجابات خاطئة.

---

## ٦. الأخطاء التي وقعت فعلاً — لا تكرّرها

كلٌّ منها كلّف وقتاً، وبعضها كان سيكسر امتحاناً على طالب.

### `PGRST102: All object keys must match`

`upsert` بدفعة صفوفها مختلفة المفاتيح يُرفض كاملاً. وكان الكود يعيدها إلى
الطابور **بالتركيبة نفسها** فتفشل مجدداً: تعطّلٌ دائم لا يُحفظ بعده شيء.

الحل في [`exam-runner.tsx`](src/app/(student)/exams/[examId]/exam-runner.tsx):
التقسيم حسب مجموعة الحقول وإرسال كل مجموعة وحدها.

**ولا توحّد المفاتيح بملء الناقص بـ `null`** — ذلك يمحو صورة الطالب حين
يعدّل نصه. `scripts/check-answer-save.mjs` يثبت هذا.

### `PGRST201: ambiguous embed`

جدولان بمفتاحين أجنبيين إلى `profiles` يجعلان `profiles(...)` ملتبساً.
النتيجة **ليست عطلاً ظاهراً** بل قائمة فارغة: `data ?? []` يبتلع الخطأ.
اكتشفها المدرّس بنفسه — صفحة التصحيح كانت فارغة وطالب ينتظر.

الحل: تسمية المفتاح صراحةً
`profiles!exam_attempts_student_id_fkey(...)`، و`scripts/check-queries.mjs`
يشغّل كل استعلامات المشروع على المشروع الحقيقي ليمسك أمثالها.

### حدّ ١٠٠٠ صف في PostgREST — بلا خطأ

`from("questions").select(...)` بلا فلتر يعيد أول ألف صف ويسكت. الصفحة
تحسب العدد من هذه العيّنة فتكتب «٠ سؤال» وداخل البنك الاثنان والعشرون
موجودون. اكتشفها المدرّس على صفحة بنك الأسئلة في لوحة المدرّس بعد أن
صار في الجدول ١١٥٧ سؤالاً.

الحل: فلتر `exam_id` على البنوك المطلوبة (كما في صفحة بنك الطالب)، وافحص
`error`. القصّ لا يملأ `error` — فلذلك `data ?? []` هنا لا يبتلع فشلاً
بل يبتلع **نجاحاً ناقصاً**.

### تحويل enum ناقص

```sql
status = case when v_has_essay then 'submitted' else 'graded' end::public.attempt_status
```

بلا `::public.attempt_status` كان **كل تسليم في المنصة سيفشل**. لم يظهر
إلا بتشغيل `08_smoke_test.sql` فعلاً. المترجم لا يمسك هذا ولا القراءة.

### علامة `$` في `String.replace`

`replace()` تعامل `$` في نص البديل معاملة خاصة، فأفسدت `01_schema.sql`
مرتين وتركت enums غير معلنة وأعمدة تشير إليها. استعمل
`split().join()` أو أدوات التحرير، لا `replace` على نصوص فيها `$`.

### ساعة جهاز الطالب

كان `updated_at` يُرسل من المتصفح. ساعة جهاز طالبة عندنا متأخرة **إحدى
عشرة ساعة**، فبياناتها تقول إن إجاباتها حُفظت قبل أن يبدأ الامتحان. صار
مُحفِّزاً في قاعدة البيانات. **لا تأتمن المتصفح على وقت.**

### الشبكة تموت قبل الجهاز

انقطعت الكهرباء على طالبة وهي تكتب مقالاً. اختياراتها كانت محفوظة
والمقالي ضاع — لأن **الاختياري يحتاج نجاح حفظ واحداً في عمره، والمقالي
يحتاج نجاحاً في اللحظة الأخيرة** إذ تُكتب قيمته فوق نفسها كل ثانية.
والراوتر يموت قبل اللابتوب فتظل تكتب والشبكة مقطوعة.

الحل: [`exam-draft.ts`](src/lib/exam-draft.ts) — مسودة في `localStorage`
تُكتب على القرص فوراً بلا شبكة. **ولا تُطبَّق من تلقائها**: قد يكون الطالب
مسح ما كتبه عمداً، وإحياؤه من ورائه أسوأ من فقدانه. تُعرض ويُترك القرار له.

### الحذف المتسلسل

`chapters → lessons → exams → exam_attempts → answers` كلها
`on delete cascade`. حذف فصل واحد بلا حارس كان سيمحو **٤٠ إجابة في ٣
محاولات** — قيس فعلاً لا خُمِّن. لذلك الحذف النهائي مشروط: مؤرشف **و**
فارغ.

---

## ٧. Next.js 16 — ما يخالف ما تعرفه

- **`src/proxy.ts` بديل `middleware.ts`** (مهجور في 16). النوعان
  `NextProxy` و`ProxyConfig` من `next/server`.
- **المسارات مطبوعة الأنواع**: صفحة جديدة لا تُعرف أنواعها حتى تشغّل
  `npm run build` مرة. خطأ `does not satisfy the constraint 'AppRoutes'`
  يعني «ابنِ أولاً» لا «الكود خطأ».
- **Tailwind v4**: `@theme` في CSS، لا ملف `tailwind.config`.
- `params` و`searchParams` **وعود** — `await` قبل الاستعمال.
- التوثيق الحقيقي في `node_modules/next/dist/docs/`. اقرأه ولا تعتمد على
  ذاكرتك عن Next 14.

### Supabase

- `@supabase/ssr` بـ `getAll`/`setAll` للكوكيز.
- الوسيط **يجب** أن ينتظر `supabase.auth.getUser()` قبل إرجاع الاستجابة.
- نظام المفاتيح الجديد: `sb_publishable_…` للمتصفح و`sb_secret_…`
  للسيرفر وحده. المتغيّر اسمه
  `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` لا `ANON_KEY`.
- الجلسات طويلة عمداً (`persistSession` و`autoRefreshToken`): الطالب يبقى
  مسجّلاً شهوراً.

---

## ٨. سير العمل

### قبل كل دفع

```bash
npm run check          # أنواع + lint + مدقّق SQL
npm run check:queries  # كل استعلامات المشروع على المشروع الحقيقي
npm run check:types    # منطق الأنواع الجديدة والبعثرة   → 28/28
npm run check:save     # حفظ الإجابات على المشروع الحقيقي → 6/6
npm run build
```

وفحوص SQL (تحتاج `SUPABASE_DB_URL` من `.env.local`):

```bash
psql "$SUPABASE_DB_URL" -f supabase/sql/07_security_check.sql        # 15/15
psql "$SUPABASE_DB_URL" -f supabase/sql/08_smoke_test.sql            # 13/13
psql "$SUPABASE_DB_URL" -f supabase/sql/11_model_answer_check.sql    #   5/5
psql "$SUPABASE_DB_URL" -f supabase/sql/17_new_types_smoke.sql       # 12/12
psql "$SUPABASE_DB_URL" -f supabase/sql/19_bank_security_check.sql   # 15/15
psql "$SUPABASE_DB_URL" -f supabase/sql/24_bank_levels_check.sql     # 11/11
```

كلها داخل معاملات تنتهي بـ `rollback` ولا تترك أثراً.

### رسائل الـ commit

بالعربية، وتقول **لماذا** لا ماذا. الكود يقول ماذا. شاهد `git log` —
هذا ما يجعل المشروع مفهوماً لمن يأتي بعدك، وهو المصدر الوحيد لأسباب
القرارات إن ضاع هذا الملف.

### التحقق قبل الادّعاء

لا تقل «تمام» قبل أن تشغّل. أخطر عيبين في تاريخ هذا المشروع (تحويل الـ
enum و`PGRST201`) كانا يبدوان سليمين في القراءة تماماً.

---

## ٩. الحالة الآن

### جاهز ويعمل

مصادقة وصلاحيات لكل عنصر · محتوى ثلاثي المستوى · رفع PDF بروابط موقّعة ·
ثمانية أنواع أسئلة · استيراد JSON بتحقق كامل · محرّك امتحان بحفظ تلقائي
ومسودة محلية · تصحيح آلي في Postgres · تصحيح المقالي يدوياً بملاحظات ·
بنك أسئلة بتصحيح فوري · تشخيص للمدرّس · حارس مساحة تخزين · نبضة إبقاء
Supabase حية.

**المحتوى:** ٧ فصول و٢٣ درساً في البذرة. ٥٥٨ سؤالاً مستورداً (٢٥٣ في
امتحانات و٣٠٥ في تدريبات). الفصول ١ و٢ مكتملة.

### معلّق

- الدروس من ٣-١ فصاعداً بلا محتوى.
- بنك الأسئلة **مبنيّ ولم يُملأ** — لا أسئلة فيه بعد. المستويات والفلتر
  ووضع المراجعة جاهزة، فالأسئلة تُكتب على شكلها النهائي من أول مرة.
- ربط Vercel وضبط المتغيرات في البيئات الثلاث.
- إعدادات جلسات Supabase (Inactivity timeout و Time-box مطفأان) وتفعيل
  Leaked Password Protection.
- «إظهار الإجابات» مفتوح على ١٢ عنصراً من أيام التجربة — يجب إغلاقه قبل
  دخول الطلاب.

### قاعدة كتابة أسئلة البنك

**لا يُنسخ سؤال من امتحان أو تدريب إلى البنك.** البنك يعطي الإجابة فوراً،
فأول ما يدخله سؤال امتحان يموت الامتحان. الأسئلة الـ٥٥٨ الموجودة كلها
محجوزة، وأسئلة البنك تُكتب من الصفر.

**ولكل سؤال بنك `tier` مطلوب** — يرفض المستورد الملف بدونه (وحده البنك،
لا التدريبات والامتحانات):

| القيمة | العربية | ما يطلبه السؤال |
|---|---|---|
| `definition` | تعريف | استرجاع تعريف أو تمييز مصطلح |
| `application` | تطبيق | تطبيق المفهوم على حالة لم ترد في الكتاب |
| `trap` | تفريق | التفريق بين مفهومين متقاربين يخلط بينهما الطلبة |

ليست درجات صعوبة: «سهل ومتوسط وصعب» حكمٌ من الكاتب على طالب لا يعرفه،
وهذه تصف الطلب نفسه فتُفحَص ولا تُظَن. والترتيب هو ترتيب الجلسة.

صيغة ملف الأسئلة الكاملة، بأمثلة مفحوصة على المستورد نفسه:
https://claude.ai/code/artifact/0e5a1a54-ec2f-4c94-91c4-9d79e51ee18f

---

## ١٠. ما لا تفعله

1. **لا تحذف شرط `kind <> 'bank'`** من `check_bank_answer`.
2. **لا تمنح `grade_one`** لأحد.
3. **لا تضف عمود صحة** إلى `question_options`.
4. **لا تبعثر وقت العرض** بدل وقت التخزين.
5. **لا تنسخ منطق التصحيح** — نادِ `grade_one`.
6. **لا تعدّل ملف SQL قديماً** ليغيّر سلوكاً. أضف رقماً أعلى.
7. **لا تعتمد على `data ?? []`** لابتلاع خطأ استعلام. افحص `error`.
8. **لا تجعل `bank_progress.state` ينزل** من `correct`. النسيان في `forgot`.
8. **لا تضف لوحة صدارة** ولا نقاطاً ولا سلاسل.
9. **لا ترفع `curriculum/`** إلى git — الريبو عام والكتاب محفوظ الحقوق.
10. **لا تقل «اتصلح»** قبل أن تشغّل الفحص وترى الرقم.
