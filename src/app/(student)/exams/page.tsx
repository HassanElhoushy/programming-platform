import { redirect } from "next/navigation";

/**
 * كانت هذه الصفحة تعرض التدريبات والامتحانات في قائمة واحدة، وهي نفسها
 * الموجودة تحت "المحتوى" بترتيب مختلف. حلّ محلها بنك الأسئلة في شريط
 * التنقّل، ويبقى المسار محوَّلاً لئلا ينكسر رابط قديم في يد طالب.
 */
export default function ExamsIndexPage() {
  redirect("/bank");
}
